# Spring Boot 配置
server:
  port: 9092  # 应与 servicecomb.rest.address 相同才能使用 Web 容器

servicecomb:
  service:
    application: servicecomb-practice
    name: user-service
    version: 0.0.1
    registry:
      address: http://127.0.0.1:30100

  rest:
    address: 0.0.0.0:9092